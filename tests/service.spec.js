/*
describe('When all trips are loaded', function() {
  var traineesFactory;

  beforeEach(module('factories'));
  beforeEach(module(function($provide) {
      $provide.service('traineesFactory', trainees);
  }));
  beforeEach(inject(function($injector) {
    traineesFactory = $injector.get('traineesFactory');
  }));

  it('should return a list with two items', function() {
    expect(traineesFactory.getAll().length).toEqual(3);
  });
});
*/
